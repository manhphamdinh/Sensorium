<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/ll"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:clipChildren="false"
    android:clipToPadding="false"
    android:orientation="vertical"
    tools:context=".Puzzle17Fragment">

    <ImageView
        android:id="@+id/imageView0"
        style="@style/centerBoxInChallenge"
        android:backgroundTint="@color/puzzle17"
        android:layout_centerInParent="true"/>

    <TextView
        android:id="@+id/tvUndo"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_toStartOf="@id/imageView0"
        android:text="Undo"
        android:textSize="20sp" />

    <TextView
        android:id="@+id/tvThis"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="this."
        android:layout_centerVertical="true"
        android:layout_toEndOf="@id/imageView0"
        android:textSize="20sp"/>
</RelativeLayout>